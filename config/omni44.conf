# Bluetooth TX power
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# The Omni44 PCB includes optional nice!view and joystick footprints. They are
# intentionally left disabled until their ZMK drivers are verified separately.
